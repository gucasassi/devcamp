{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [2, "always", ["deps", "health", "release", "setup", "bootcamp", "course", "mongodb", "colors", "middleware", "seeder"]]
  }
}
